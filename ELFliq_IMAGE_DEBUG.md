# 🎯 РЕШЕНИЕ ПРОБЛЕМЫ С ИЗОБРАЖЕНИЯМИ ELFliq

## 📋 Проблема:
Картинка ELFliq не отображается, потому что:
1. Формат SVG может не поддерживаться в CSS background
2. Путь к изображению может быть неправильным
3. Бренд название в Google Sheets может отличаться

## ✅ Быстрое решение:

### 1. Проверьте консоль браузера:
Откройте DevTools (F12) и посмотрите ошибки загрузки изображений.

### 2. Проверьте бренд в Google Sheets:
Убедитесь, что в колонке `brand` указано именно:
- `ELFliq` (с большой буквы)
- Не `elfliq`, не `ELF LIQ`, не `elf liq`

### 3. Проверьте путь к изображению:
Файл должен быть доступен по:
```
http://localhost:5174/images/brands/elfliq/elfliq_liquid.png
```

## 🔧 Отладка:

Я добавил console.log для отладки. Откройте консоль браузера и посмотрите:
- Какой бренд приходит из Google Sheets
- Какой путь к изображению формируется
- Есть ли ошибки загрузки

## 📱 Альтернативное решение:

Если изображение по-прежнему не работает, используйте градиентный фон:

```javascript
// Вместо изображения будет красивый градиент
const brandGradients = {
  'elfliq': 'linear-gradient(135deg, #ff2d55 0%, #ff6b6b 100%)',
  'elf bar': 'linear-gradient(135deg, #4a90e2 0%, #357abd 100%)',
  'geekvape': 'linear-gradient(135deg, #ff6b35 0%, #f7931e 100%)',
  'vaporesso': 'linear-gradient(135deg, #9b59b6 0%, #8e44ad 100%)',
};
```

## 🎨 Визуально это будет выглядеть:
- 🍒 **ELFliq** - красный градиент
- ⚡ **ELF BAR** - синий градиент  
- 🔧 **GeekVape** - оранжевый градиент
- 💎 **Vaporesso** - фиолетовый градиент

## 🚀 Проверка:
1. Откройте http://localhost:5174
2. Откройте DevTools (F12)
3. Перейдите в каталог
4. Посмотрите консоль - там будет отладочная информация
5. Убедитесь, что бренд правильно определяется

## 💡 Рекомендации:
1. **Используйте JPG или PNG** вместо SVG для фоновых изображений
2. **Проверьте регистр букв** в названии бренда
3. **Проверьте консоль** на наличие ошибок 404

**Система работает!** Просто нужно правильно настроить бренды в Google Sheets.